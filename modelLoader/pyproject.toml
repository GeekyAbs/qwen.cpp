[project]
name = "modelloader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "geekyabs", email = "abeshchakraborty10@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "torch>=2.14.0",
    "transformers>=5.17.0",
]

[project.scripts]
modelloader = "modelloader:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
